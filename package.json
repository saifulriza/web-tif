{
  "scripts": {
    "start": "snowpack dev",
    "build": "purgecss --css src/css/bootstrap.css --content public/index.html --output public/assets/css && snowpack build",
    "min": "purgecss --css src/css/bootstrap.css --content index.html --output public/",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1",
    "format": "prettier --write \"src/**/*.js\"",
    "lint": "prettier --check \"src/**/*.js\""
  },
  "dependencies": {
    "bootstrap": "^4.5.3",
    "canvas-confetti": "^1.2.0",
    "cross-env": "^7.0.2",
    "npm-run-all": "^4.1.5",
    "rellax": "^1.12.1",
    "typed.js": "^2.0.11"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^3.0.0",
    "postcss-cli": "^8.3.0",
    "prettier": "^2.0.5",
    "snowpack": "^2.17.1"
  }
}
